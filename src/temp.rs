/// Cộng thêm 1 vào một số.
///
/// # Examples
///
/// ```
/// let arg = 5;
/// let answer = crate::add_one(arg);
///
/// assert_eq!(6, answer);
/// ```
#[allow(dead_code)]
pub fn add_one(x: i32) -> i32 {
    x + 1
}
